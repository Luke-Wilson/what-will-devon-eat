version: '3.3'
services:
  # app:
  #   build:
  #     context: ./app
  #   ports:
  #     - 8080:8080
  #   volumes:
  #     - type: bind
  #       source: ./app
  #       target: /wwwroot
  #   environment:
  #     CHOKIDAR_USEPOLLING: 'true'

  server:
    build:
      context: ./server
    # image: 'wwde_server'
    ports:
      - 4000:4000
    volumes:
      - type: bind
        source: ./server
        target: /wwwroot
    environment:
      # CHOKIDAR_USEPOLLING: 'true'

# volumes:
#   data-db:
